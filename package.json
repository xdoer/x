{
  "name": "x",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*",
      "examples/*"
    ]
  },
  "scripts": {
    "dev": "lerna run --parallel --stream dev",
    "build": "lerna run --stream build",
    "test": "lerna run --parallel --stream test",
    "publish": "lerna publish --force-publish=*",
    "format": "prettier --write ."
  },
  "eslintConfig": {
    "extends": "tie-app"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/xdoer/x.git"
  },
  "author": "@xdoer",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/xdoer/x/issues"
  },
  "homepage": "https://github.com/xdoer/x#readme",
  "devDependencies": {
    "@types/prettier": "^2.2.3",
    "eslint-config-tie-app": "0.0.5",
    "husky": "^6.0.0",
    "lerna": "^4.0.0",
    "prettier": "^2.2.1",
    "tsdx": "^0.14.1",
    "tslib": "^2.2.0",
    "typescript": "^4.2.4"
  }
}
